## 2024-05-23 - [GitHub Profile Accessibility]
**Learning:** READMEs often rely on decorative images like separators that lack `alt=""`, creating noise for screen readers.
**Action:** Always audit `<img>` tags in READMEs; if decorative, use `alt=""`; if informative, describe the content.
